import { OpenItemdetailsDirective } from './open-itemdetails.directive';

describe('OpenItemdetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenItemdetailsDirective();
    expect(directive).toBeTruthy();
  });
});
