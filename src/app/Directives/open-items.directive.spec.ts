import { OpenItemsDirective } from './open-items.directive';

describe('OpenItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenItemsDirective();
    expect(directive).toBeTruthy();
  });
});
